import { SkillsWorkspace } from "../_components/SkillsWorkspace";

/* Route: /skills/select — master-detail with nothing selected (landing after a skill is deleted). */
export default function SkillsSelectPage() {
  return <SkillsWorkspace />;
}
