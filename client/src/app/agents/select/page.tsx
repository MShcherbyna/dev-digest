import { AgentsWorkspace } from "../_components/AgentsWorkspace";

/* Route: /agents/select — master-detail with nothing selected (landing after the open agent is deleted). */
export default function AgentsSelectPage() {
  return <AgentsWorkspace />;
}
